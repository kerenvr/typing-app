/** @type {string[]} */

export const publicRoutes = [
    "/",
    "/auth/new-verification"
]

/** @type {string[]} */
export const authRoutes = [
    "/auth/login",
    "/auth/register",
    "/auth/forgot-password",
]

/** @type {string[]} */
export const apiAuthPrefix = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT_URL = "/account"